{% extends 'base_dashboard.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <h1 class="dashboard-title">Edit Post</h1>

    <div class="dashboard-table">
        {{ include('user/post/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <div class="bg-light w-25 m-auto mb-5 d-flex justify-content-around align-items-center">
        <a class="text-decoration-none text-secondary" href="{{ path('app_post_index', {userId: app.user.id}) }}">back to list</a>
        {{ include('user/post/_delete_form.html.twig') }}
    </div>

{% endblock %}
