{% extends 'base_dashboard.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1 class="dashboard-title">Détail du post</h1>

    <table class="dashboard-table table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ post.picture }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ post.description }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ post.creationDate ? post.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <div class="bg-light w-25 m-auto d-flex justify-content-around align-items-center">
        <a class="text-decoration-none text-secondary" href="{{ path('app_admin_post_index') }}">back to list</a>
        <a class="text-decoration-none text-secondary" href="{{ path('app_admin_post_edit', {'id': post.id}) }}">edit</a>
        {{ include('admin_post/_delete_form.html.twig') }}
    </div>
{% endblock %}
